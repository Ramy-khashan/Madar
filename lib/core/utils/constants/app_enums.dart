enum RouterType { goName, pushName, relacementName, pushReplacementNamed }

enum RequestStatus { init, loading, success, failed }

enum RequestStatusType { pending, confirmed, reject, inProgress, completed, cancelled, refunded }
  
enum RequestServiceType { doctorVisit, medicalTest, nursingCare }
enum LocalEnumKey{accessToken ,refreshToken  }
enum ToastStates { success, warning, error, info }
enum AuctionDepositPaymentMethod { applePay, visa }
enum SubscriptionPaymentMethod { applePay, visa }
enum SubscriptionBillingCycle { monthly, yearly }
import 'package:flutter/material.dart';

class VillaDetailsItem extends StatelessWidget {
  const VillaDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class ApartmentDetailsItem extends StatelessWidget {
  const ApartmentDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class FloorDetailsItem extends StatelessWidget {
  const FloorDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class TownhouseDetailsItem extends StatelessWidget {
  const TownhouseDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class BuildingDetailsItem extends StatelessWidget {
  const BuildingDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class LandDetailsItem extends StatelessWidget {
  const LandDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class RestHouseDetailsItem extends StatelessWidget {
  const RestHouseDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class TowerDetailsItem extends StatelessWidget {
  const TowerDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class ShopDetailsItem extends StatelessWidget {
  const ShopDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class OfficeDetailsItem extends StatelessWidget {
  const OfficeDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class FarmDetailsItem extends StatelessWidget {
  const FarmDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}

class WarehouseDetailsItem extends StatelessWidget {
  const WarehouseDetailsItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [

    ],);
  }
}
